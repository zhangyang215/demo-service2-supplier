spring:
  application:
    name: service-supplier
  profiles:
      active: dev
  cloud:
    config:
      uri: ${GIT_URL:http://localhost:9000/}
      failFast: false
      # discovery:
      #   enabled: true
      #   serviceId: service-config

mybatis:
  mapperLocations: classpath*:mapper/*Mapper.xml
feign:
  hystrix:
    enabled: true
logging:
  level:
    com.demo2.supplier: DEBUG
    com.demo2.support: DEBUG

---
spring:
  profiles: dev
  sleuth:
    enabled: false
  datasource:
    url: jdbc:mysql://47.105.194.89:32306/demo
    username: root
    password: 1234
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    database: 0
    host: 47.105.178.19
    port: 31379
    password: 
    pool:
      maxActive: 200
      maxWait: -1
      maxIdel: 10
      minIdel: 0
    timeout: 1000
server:
  port: 9004   

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
---
spring:
  profiles: docker

server:
  port: ${SERVICE_PORT:9004}

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${REGISTRY_URL}