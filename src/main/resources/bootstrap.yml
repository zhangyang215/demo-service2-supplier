spring:
  application:
    name: service-supplier
  profiles:
      active: dev
  cloud:
    config:
      uri: ${GIT_URL:http://localhost:9000/}
      failFast: false
      # discovery:
      #   enabled: true
      #   serviceId: service-config

mybatis:
  mapperLocations: classpath*:mapper/*Mapper.xml
feign:
  hystrix:
    enabled: true

---
spring:
  profiles: dev
  sleuth:
    enabled: false
  datasource:
    url: jdbc:mysql://139.9.35.139:3306/demo
    username: root
    password: 1234
    type: com.alibaba.druid.pool.DruidDataSource
server:
  port: 9003   

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
---
spring:
  profiles: docker

server:
  port: ${SERVICE_PORT:9003}

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${REGISTRY_URL}